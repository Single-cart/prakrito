<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Order Notification</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #4caf50;
        color: white;
        text-align: center;
        padding: 10px;
      }
      .order-details,
      .shipping-info,
      .order-items {
        background-color: #f2f2f2;
        border: 1px solid #ddd;
        padding: 15px;
        margin-top: 20px;
      }
      .item {
        margin-bottom: 10px;
        border-bottom: 1px solid #ddd;
        padding-bottom: 10px;
      }
      .item:last-child {
        border-bottom: none;
      }
      .total {
        font-weight: bold;
        margin-top: 15px;
      }
      .image {
        max-width: 100px;
        height: auto;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>New Order Notification</h1>
    </div>
    <p>Hello Admin,</p>
    <p>A new order has been placed. Here are the details:</p>

    <div class="order-details">
      <p><strong>Order ID:</strong> <%= orderId %></p>
      <p><strong>Order Date:</strong> <%= createdAt.toLocaleString() %></p>
      <p><strong>Order Status:</strong> <%= orderStatus %></p>
      <p><strong>Payment Type:</strong> <%= paymentType %></p>
      <% if (user) { %>
      <p><strong>User Email:</strong> <%= user.email %></p>
      <% } %>
      <% if (orderNots) { %>
      <p><strong>Order Notes:</strong> <%= orderNots %></p>
      <% } %>
    </div>

    <div class="shipping-info">
      <h2>Shipping Information</h2>
      <p><strong>Full Name:</strong> <%= shippingInfo.fullName %></p>
      <p><strong>Phone:</strong> <%= shippingInfo.phone %></p>
      <p><strong>Address:</strong> <%= shippingInfo.address %></p>
    </div>

    <div class="order-items">
      <h2>Order Items</h2>
      <% orderItems.forEach(function(item) { %>
      <% 
        const product = item.product;
        const variation = product?.priceVariation?.[item.priceVariationIndex - 1];
        const productImage = product?.images?.[0];
      %>
      <div class="item">
        <% if (productImage) { %>
        <img src="<%= productImage %>" alt="Product image" class="image" />
        <% } %>
        <h3><%= product?.name || `Product #${orderItems.indexOf(item) + 1}` %></h3>
        <p><strong>Price:</strong> ৳<%= item.price %></p>
        <p><strong>Quantity:</strong> <%= item.quantity %></p>
        <% if (variation?.color) { %>
        <p><strong>Color:</strong> <%= variation.color %></p>
        <% } %>
        <% if (variation?.size) { %>
        <p><strong>Size:</strong> <%= variation.size %></p>
        <% } %>
        <% if (variation?.weight) { %>
        <p><strong>Weight:</strong> <%= variation.weight %></p>
        <% } %>
        <% if (variation?.quantity) { %>
        <p><strong>Variation Quantity:</strong> <%= variation.quantity %></p>
        <% } %>
        <% if (!variation) { %>
        <p><strong>Variation Index:</strong> <%= item.priceVariationIndex %></p>
        <% } %>
      </div>
      <% }); %>

      <p><strong>Items Price:</strong> <%= itemsPrice.toFixed(2) %> TK</p>
      <p><strong>Shipping Price:</strong> <%= shippingPrice.toFixed(2) %> TK</p>
      <p class="total">Total Amount: <%= totalAmount.toFixed(2) %> TK</p>
    </div>

    <p>Please process this order as soon as possible.</p>
    <p>Thank you,<br />Prakrito</p>
  </body>
</html>
